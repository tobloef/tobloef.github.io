<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<meta name="description" content="Software engineer by day, software engineer by night also.">
		<slot name="head" />
		<title>Tobias Løfgren</title>
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
	</head>
	<body>
		<main>
			<slot />
		</main>
	</body>
</html>

<style is:global>
	:root {
		--light-text: hsl(43, 26%, 11%);
		--light-background: hsl(36, 49%, 87%);
		--light-primary: hsl(31, 100%, 26%);
		--light-secondary: hsl(32, 100%, 17%);
		--light-accent: hsl(38, 100%, 45%);

		--dark-text: hsl(36, 49%, 83%);
		--dark-background: hsl(30, 6%, 7%);
		--dark-primary: hsl(31, 100%, 74%);
		--dark-secondary: hsl(32, 100%, 83%);
		--dark-accent: hsl(38, 100%, 45%);

		--text: var(--light-text);
		--background: var(--light-background);
		--primary: var(--light-primary);
		--secondary: var(--light-secondary);
		--accent: var(--light-accent);
	}

	@media (prefers-color-scheme: dark) {
		:root {
			--text: var(--dark-text);
			--background: var(--dark-background);
			--primary: var(--dark-primary);
			--secondary: var(--dark-secondary);
			--accent: var(--dark-accent);
		}
	}

	html,
	body {
		margin: 0;
		width: 100%;
		min-height: 100vh;

		font-family: Inter, sans-serif;
		font-size: 1.02em;
		line-height: 1.8;

		background: var(--background);
		color: var(--text);
	}

	main {
		margin: 0 auto;
		padding: 0 2em 2em 2em;
		max-width: 80ch;
		display: flex;
		flex-direction: column;
	}

	img {
		width: auto;
		max-width: 100%;
		height: auto;
		align-self: center;
		margin: 1em 0 2em 0;
		max-height: 800px;
	}

	a {
		color: inherit;
		text-decoration: none;
		font-size: inherit;
		background-position-y: -0%;
		background-image: linear-gradient(180deg, transparent 50%, var(--accent) 50%);
		transition: all 0.1s ease;
		background-size: auto 185%;
		padding: 3px 0;
	}

	@media (prefers-color-scheme: dark) {
		a {
			background-size: auto 190%
		};
	}

	a:hover {
		background-position-y: 5%;
		transition: all 0.1s ease;
	}

	a[href^="#"] {
		background-image: none;
	}

	blockquote {
		border: 1px solid var(--accent);
		border-left: 8px solid var(--accent);
		margin: 0;
		background: var(--secondary);
		color: var(--background);
		padding: 0 1em;
		border-radius: 2px;
	}

	h2 + p,
	h3 + p,
	h4 + p,
	h2 + ul,
	h3 + ul,
	h4 + ul{
		margin-top: 0;
	}

	h2 {
		margin-bottom: 0.3em;
	}

	p + ul {
		margin-top: 0;
	}

	.de-emphasize {
		opacity: 0.7;
		font-style: italic;
	}

	code {
		line-break: anywhere;
	}
</style>
