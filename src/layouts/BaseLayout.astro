<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Tobias Løfgren</title>
		<slot name="head" />
	</head>
	<body>
		<main>
			<slot />
		</main>
	</body>
</html>

<style is:global>
	:root {
		--text: hsl(43, 26%, 11%);
		--background: hsl(36, 49%, 83%);
		--primary: hsl(31, 100%, 26%);
		--secondary: hsl(32, 100%, 17%);
		--accent: hsl(38, 100%, 45%);
	}

	@media (prefers-color-scheme: dark) {
		:root {
			--text: hsl(36, 49%, 83%);
			--background: hsl(30, 6%, 7%);
			--primary: hsl(31, 100%, 74%);
			--secondary: hsl(32, 100%, 83%);
			--accent: hsl(38, 100%, 45%);
		}
	}

	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;

		font-family: Inter, sans-serif;
		font-size: 1.02em;
		line-height: 1.8;

		background: var(--background);
		color: var(--text);
	}

	main {
		margin: 0 auto;
		padding: 0 2em 2em 2em;
		max-width: 80ch;
	}

	img {
		max-width: 100%;
		height: auto;
	}

	a {
		color: inherit;
		text-decoration: none;
		font-size: inherit;
		background-position-y: -0%;
		background-image: linear-gradient(180deg, transparent 50%, var(--accent) 50%);
		transition: all 0.1s ease;
		background-size: auto 185%;
		padding: 3px 0;
	}

	@media (prefers-color-scheme: dark) {
		a {
			background-size: auto 190%
		};
	}

	a:hover {
		background-position-y: 5%;
		transition: all 0.1s ease;
	}

	a[href^="#"] {
		background-image: none;
	}
</style>
